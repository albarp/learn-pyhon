version: '3.8'
services:
  app:
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
    image: learn-python-app:0.0.6
    container_name: learn-python-app-container
    volumes:
      - ..:/mnt
    ports:
      - "3000:3000"
    environment:
      - PYTHONUNBUFFERED=1
    command: /bin/sh -c "while sleep 1000; do :; done"